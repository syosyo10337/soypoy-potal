# 開発環境用 .dockerignore

# 重い依存関係（コンテナ内で再インストール）
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ビルド出力（開発時は動的生成）
dist/
build/
.next/
.nuxt/
out/

# 環境設定（機密情報を含む可能性）
.env.local
.env.production
.env.test
# 開発環境では .env.development は含めることもある

# IDEファイル（開発者固有）
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# OS生成ファイル
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ログファイル
logs/
*.log

# テスト結果（開発時は動的生成）
coverage/
.nyc_output/
test-results/

# 一時ファイル
tmp/
temp/

# Git（履歴は重いため除外）
.git/

# CI/CD（開発環境では不要）
.github/
.gitlab-ci.yml
.travis.yml
.circleci/

# Docker関連（再帰的ビルドを防ぐ）
Dockerfile*
docker-compose*.yml
.dockerignore

# Cloudflare Workers関連（機密情報）
.wrangler/
wrangler.toml.backup

# 開発環境では含めたいファイル
# README.md は含める（開発者向け情報）
# package.json は含める（必須）
# tsconfig.json は含める（TypeScript設定）
# .eslintrc* は含める（コードフォーマット）
# .prettierrc* は含める（コードフォーマット）